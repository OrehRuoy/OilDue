# godot-photopicker

Godot iOS PhotoPicker from godot-sdk-integrations/godot-ios-plugins, with PR #105 applied (present on `GDTAppDelegateService.viewController` for Godot 4.6).

Build on macOS via `native/build_plugins.sh`. Enable **PhotoPicker** on the iOS export preset only.

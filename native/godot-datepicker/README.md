# godot-datepicker

In-tree Godot 4.6 iOS plugin. Presents `UIDatePicker` (date-only wheels) on `GDTAppDelegateService.viewController` (PR #105).

Build on macOS via `native/build_plugins.sh`. Enable **DatePicker** on the iOS export preset only. No camera or photo privacy strings.
